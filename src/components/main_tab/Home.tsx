import { StyleSheet, Text, TouchableOpacity, View } from 'react-native'
import React from 'react'
import MainHeader from '../../components/header/MainHeader'
import colors from '../../styles/colors'
import Feather from 'react-native-vector-icons/Feather'
import { paddingHorizontal } from '../../styles'
import { HStack } from 'native-base'
import { FlatListScroll } from '../custom/Flatlist'

const Home = () => {
//   const rightIcon = () => (
//     <TouchableOpacity>

//     </TouchableOpacity>
// );
  return (
    <>
      <MainHeader 
      title={'Eat 24'}
      rightIcon={'notifications-outline'}/>
      <FlatListScroll>
      <View style={styles.container}>
      <TouchableOpacity 
          style={styles.search}>
          <HStack>
          <Feather name='search' size={20} color={colors.iconColor} />
          <Text style={{marginLeft:5}}>Find food or Restuarant</Text>
          </HStack>
        <View style={styles.menu}>
        <Feather name='align-center' size={20} color={colors.baseColor} />
        </View>
        </TouchableOpacity>
      </View>


      <View style={styles.container}>
      <TouchableOpacity 
          style={styles.search}>
          <HStack>
          <Feather name='search' size={20} color={colors.iconColor} />
          <Text style={{marginLeft:5}}>Find food or Restuarant</Text>
          </HStack>
        <View style={styles.menu}>
        <Feather name='align-center' size={20} color={colors.baseColor} />
        </View>
        </TouchableOpacity>
      </View>
      <View style={styles.container}>
      <TouchableOpacity 
          style={styles.search}>
          <HStack>
          <Feather name='search' size={20} color={colors.iconColor} />
          <Text style={{marginLeft:5}}>Find food or Restuarant</Text>
          </HStack>
        <View style={styles.menu}>
        <Feather name='align-center' size={20} color={colors.baseColor} />
        </View>
        </TouchableOpacity>
      </View>
      <View style={styles.container}>
      <TouchableOpacity 
          style={styles.search}>
          <HStack>
          <Feather name='search' size={20} color={colors.iconColor} />
          <Text style={{marginLeft:5}}>Find food or Restuarant</Text>
          </HStack>
        <View style={styles.menu}>
        <Feather name='align-center' size={20} color={colors.baseColor} />
        </View>
        </TouchableOpacity>
      </View>
      <View style={styles.container}>
      <TouchableOpacity 
          style={styles.search}>
          <HStack>
          <Feather name='search' size={20} color={colors.iconColor} />
          <Text style={{marginLeft:5}}>Find food or Restuarant</Text>
          </HStack>
        <View style={styles.menu}>
        <Feather name='align-center' size={20} color={colors.baseColor} />
        </View>
        </TouchableOpacity>
      </View>
      <View style={styles.container}>
      <TouchableOpacity 
          style={styles.search}>
          <HStack>
          <Feather name='search' size={20} color={colors.iconColor} />
          <Text style={{marginLeft:5}}>Find food or Restuarant</Text>
          </HStack>
        <View style={styles.menu}>
        <Feather name='align-center' size={20} color={colors.baseColor} />
        </View>
        </TouchableOpacity>
      </View>
      <View style={styles.container}>
      <TouchableOpacity 
          style={styles.search}>
          <HStack>
          <Feather name='search' size={20} color={colors.iconColor} />
          <Text style={{marginLeft:5}}>Find food or Restuarant</Text>
          </HStack>
        <View style={styles.menu}>
        <Feather name='align-center' size={20} color={colors.baseColor} />
        </View>
        </TouchableOpacity>
      </View>
      <View style={styles.container}>
      <TouchableOpacity 
          style={styles.search}>
          <HStack>
          <Feather name='search' size={20} color={colors.iconColor} />
          <Text style={{marginLeft:5}}>Find food or Restuarant</Text>
          </HStack>
        <View style={styles.menu}>
        <Feather name='align-center' size={20} color={colors.baseColor} />
        </View>
        </TouchableOpacity>
      </View>
      <View style={styles.container}>
      <TouchableOpacity 
          style={styles.search}>
          <HStack>
          <Feather name='search' size={20} color={colors.iconColor} />
          <Text style={{marginLeft:5}}>Find food or Restuarant</Text>
          </HStack>
        <View style={styles.menu}>
        <Feather name='align-center' size={20} color={colors.baseColor} />
        </View>
        </TouchableOpacity>
      </View>
      <View style={styles.container}>
      <TouchableOpacity 
          style={styles.search}>
          <HStack>
          <Feather name='search' size={20} color={colors.iconColor} />
          <Text style={{marginLeft:5}}>Find food or Restuarant</Text>
          </HStack>
        <View style={styles.menu}>
        <Feather name='align-center' size={20} color={colors.baseColor} />
        </View>
        </TouchableOpacity>
      </View>
      <View style={styles.container}>
      <TouchableOpacity 
          style={styles.search}>
          <HStack>
          <Feather name='search' size={20} color={colors.iconColor} />
          <Text style={{marginLeft:5}}>Find food or Restuarant</Text>
          </HStack>
        <View style={styles.menu}>
        <Feather name='align-center' size={20} color={colors.baseColor} />
        </View>
        </TouchableOpacity>
      </View>
      <View style={styles.container}>
      <TouchableOpacity 
          style={styles.search}>
          <HStack>
          <Feather name='search' size={20} color={colors.iconColor} />
          <Text style={{marginLeft:5}}>Find food or Restuarant</Text>
          </HStack>
        <View style={styles.menu}>
        <Feather name='align-center' size={20} color={colors.baseColor} />
        </View>
        </TouchableOpacity>
      </View>
      <View style={styles.container}>
      <TouchableOpacity 
          style={styles.search}>
          <HStack>
          <Feather name='search' size={20} color={colors.iconColor} />
          <Text style={{marginLeft:5}}>Find food or Restuarant</Text>
          </HStack>
        <View style={styles.menu}>
        <Feather name='align-center' size={20} color={colors.baseColor} />
        </View>
        </TouchableOpacity>
      </View>
      <View style={styles.container}>
      <TouchableOpacity 
          style={styles.search}>
          <HStack>
          <Feather name='search' size={20} color={colors.iconColor} />
          <Text style={{marginLeft:5}}>Find food or Restuarant</Text>
          </HStack>
        <View style={styles.menu}>
        <Feather name='align-center' size={20} color={colors.baseColor} />
        </View>
        </TouchableOpacity>
      </View>
      <View style={styles.container}>
      <TouchableOpacity 
          style={styles.search}>
          <HStack>
          <Feather name='search' size={20} color={colors.iconColor} />
          <Text style={{marginLeft:5}}>Find food or Restuarant</Text>
          </HStack>
        <View style={styles.menu}>
        <Feather name='align-center' size={20} color={colors.baseColor} />
        </View>
        </TouchableOpacity>
      </View>
      </FlatListScroll>
    </>
  )
}

export default Home

const styles = StyleSheet.create({
  container:{
    backgroundColor:colors.whiteColor,
    marginHorizontal:10,
    marginTop:15,
    height:50,
    borderRadius:5
  },
  search: {
    backgroundColor: colors.whiteColor,
    flex: 1,
    height: 50,
    borderRadius: 5,
    paddingHorizontal,
    alignItems: 'center',
    flexDirection:'row',
    justifyContent:'space-between'
  },
  menu:{
   
  }
})